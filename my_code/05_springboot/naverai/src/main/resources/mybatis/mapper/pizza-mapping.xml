<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="chatbot.PizzaDAO">
  
  	<insert id="orderPizza" parameterType="pizzaDTO">
  		insert into pizza (kind, size, telnum, price, ordertime) values (#{kind}, #{size}, #{telnum}, #{price}, now())
  	</insert>
 
  </mapper>